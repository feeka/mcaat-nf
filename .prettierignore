# Files prettier must not touch.
#
# Mirrors the nf-core/tools 4.0.2 template, plus the MCAAT-specific entries
# noted below.

email_template.html
adaptivecard.json
slackreport.json
.nextflow*
work/
data/
results/
.DS_Store
testing/
testing*
*.pyc
bin/
ro-crate-metadata.json

# nf-test artefacts. Snapshots are machine generated, ordered and md5-keyed by
# nf-test itself; reformatting them changes nothing semantically but produces
# enormous, unreviewable diffs on every snapshot refresh.
.nf-test/
.nf-test.log
*.snap

# Fixture files reproduce MCAAT's byte-exact output: CRISPR_Arrays_*.txt uses
# exactly eight leading spaces then a single TAB on every spacer line, and a
# blank line terminates each array block. Reformatting them silently breaks the
# parser tests they exist to protect.
tests/fixtures/

# Vendored, maintained upstream in nf-core/modules.
modules/nf-core/
subworkflows/nf-core/

# Auto-generated by `nf-core pipelines create` / `nf-core modules install`.
conf/containers_*.config
